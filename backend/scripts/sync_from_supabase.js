#!/usr/bin/env node
/**
 * ⚠️  DEPRECADO: Este script solo sincroniza la tabla usuarios.
 * 
 * USAR EN SU LUGAR:
 * → node scripts/db_sync_from_supabase.js (sincroniza TODA la base de datos)
 * 
 * Este script se mantiene solo por compatibilidad con proyectos anteriores.
 */

console.log('⚠️  Este script está deprecado.');
console.log('\n✅ Usa en su lugar: node scripts/db_sync_from_supabase.js');
console.log('   Esto sincronizará TODA tu base de datos (todas las tablas).\n');
process.exit(0);
